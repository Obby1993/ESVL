
  <h1 class="titre_partie">TOUS LES  EVENEMENTS</h1>
   <h2> EVENEMENTS A VENIR</h2>
  <div class="list-events">
    <%@events.each do |event| %>
      <% if event.date_event - DateTime.now > 0%>
        <%=render "shared/one_event", event:event%>
      <% end %>
    <% end %>
  </div>
    <h2>EVENEMENTS PASSES</h2>
  <div class="list-events">
    <%@events.each do |event| %>
      <% if event.date_event - DateTime.now < 0 %>
        <%=render "shared/one_event", event:event%>
      <% end %>
        <%# <%= link_to "S'inscrire", new_event_event_team_path(event), class:"btn-show"%>
        <%# <%= button_to "Supprimer", event_path(event), method: :delete, class:"btn-show"%>
    <%end%>
  </div>



<%# <%= form_with url: commandes_path do %>
  <%# <%= hidden_field_tag 'article_id', @article.id %>
  <%# <%= submit_tag 'Acheter', class: 'btn btn-primary' %>
<%# <% end %>
